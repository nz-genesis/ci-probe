name: Dynamic Authority HITL

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dynamic-authority-hitl:
    name: revocation-pending-approval-irreversible-boundary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Execute dynamic authority experiment
        run: PYTHONPATH=. python3 dynamic_authority_hitl_test.py
