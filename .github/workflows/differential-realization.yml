name: Differential Realization

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  differential-realization:
    name: one-contract-two-mechanisms
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Execute differential realization
        run: PYTHONPATH=. python3 differential_realization_test.py
