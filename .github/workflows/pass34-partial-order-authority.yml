name: Genesis Pass 34 Partial Order Authority

on:
  push:
    paths:
      - 'probes/pass34_partial_order_authority.py'
      - '.github/workflows/pass34-partial-order-authority.yml'
  pull_request:
    paths:
      - 'probes/pass34_partial_order_authority.py'
      - '.github/workflows/pass34-partial-order-authority.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact triggering commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Verify exact SHA
        run: test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"

      - name: Compile public probe
        run: python -m py_compile probes/pass34_partial_order_authority.py

      - name: Run public probe
        run: python probes/pass34_partial_order_authority.py
