name: Primitive Removal

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  primitive-removal:
    name: remove-named-idempotency-construct
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Execute primitive removal experiment
        run: PYTHONPATH=. python3 primitive_removal_test.py
