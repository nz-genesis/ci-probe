name: Genesis Pass 32 Authority Delegation

on:
  push:
    paths:
      - 'probes/pass32_authority_delegation.py'
      - '.github/workflows/pass32-authority-delegation.yml'
  pull_request:
    paths:
      - 'probes/pass32_authority_delegation.py'
      - '.github/workflows/pass32-authority-delegation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact triggering commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Verify exact SHA
        run: test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"

      - name: Compile public probe
        run: python -m py_compile probes/pass32_authority_delegation.py

      - name: Run public probe
        run: python probes/pass32_authority_delegation.py
