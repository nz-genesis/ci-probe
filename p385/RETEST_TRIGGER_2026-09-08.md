# P385 isolated retest trigger

This marker exists only to trigger the dedicated P385 public workflow on `main` after execution-topology correction. It contains no Genesis corpus, credentials, or semantic result.

The workflow itself freezes the external ProvToolbox commit and records the execution receipt.